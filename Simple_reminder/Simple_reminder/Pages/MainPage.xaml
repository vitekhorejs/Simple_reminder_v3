<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Simple_reminder"
             x:Class="Simple_reminder.MainPage"
             Title="Simple Reminder">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="AddCategory" Order="Primary" Activated="AddCategory_Clicked" Text="Přidat Kategorii" Priority="0" />
    </ContentPage.ToolbarItems>

    <StackLayout>
        <Button Text="Button" Clicked="Add_Button" ></Button>
        <ListView x:Name="ListView" ItemTapped="GetContact" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid >
                            <Label Text="{Binding GetName}" FontAttributes="Bold" VerticalTextAlignment="Start"></Label>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>